const { User } = require("../models");

module.exports = {
  //get all users
  getUsers(req, res) {
    User.find;
  },
  //get single user
  //post new user
  //delete user
  //(b) remove user thoughts
  //post new user friend
  //delete friend
};
